# nzbfast: the crate's tests, benches and examples were not vendored,
# so their [[test]] / [[bench]] / [[example]] sections and the
# dev-dependencies they pull (criterion, liblzma) were dropped from
# this manifest - with them present `cargo test -p lzma-rust2` refuses
# the package outright. See README-nzbfast.md.
# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "lzma-rust2"
version = "0.20.0"
build = false
exclude = ["/tests/data"]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "LZMA / LZMA2 / LZIP / XZ compression ported from 'tukaani xz for java'"
homepage = "https://github.com/hasenbanck/lzma-rust2/"
readme = "README.md"
keywords = [
    "lzma",
    "lzip",
    "xz",
]
categories = [
    "compression",
    "no-std",
]
license = "Apache-2.0"
repository = "https://github.com/hasenbanck/lzma-rust2/"

[package.metadata.docs.rs]
all-features = true

[features]
default = [
    "std",
    "encoder",
    "optimization",
    "lzip",
    "xz",
]
encoder = []
lzip = []
optimization = []
std = []
xz = ["sha2"]

[lib]
name = "lzma_rust2"
path = "src/lib.rs"

[dependencies.sha2]
version = "0.11"
optional = true

