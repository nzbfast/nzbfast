fork: private perf fork of github.com/bitplane/rars, branch perf
last-synced-rev: 10e8f5b
last-synced-date: 2026-09-08
local-drift: none at the sync. On 22 Aug 2026 the whole vendored tree
  (104 local commits since the 24 Jul sync at 050758e, the NEON BLAKE2sp
  leaves among them) was ported INTO the fork as one commit, 1b84503 on
  branch perf, and sync-from-fork.sh then re-mirrored from it with an
  empty `git diff vendor/rars/src` as the proof. The fork checkout is
  the default RARS_FORK path, the upstream rars fork; it is private and
  publishing stays deferred per TODO 11.
  Any vendor commit after this date is drift again: port it to the fork
  before the next sync, or the sync deletes it.
  23 Aug 2026: 1b84503 was never pushed and the the upstream rars fork checkout
  that held it was gone, so origin/perf still sat at c586ae2. Re-cloned
  the private fork to the upstream rars fork and re-did the port as three commits on
  perf (f251247 backlog to 15fb34e27^, f17ff11 the split-STORED CRC fix,
  119c2df the 22-23 Aug vendor commits), pushed; `diff -r` of src/ and
  tests/fixtures/ against the vendored tree is empty at 119c2df. The
  fork's rar50_fixtures.rs / rar50_stream_source.rs tests were moved to
  the pad-length and RAR 1.3-only-refused contracts there.
  7 Sep 2026: those three revs were gone again - `git cat-file -t
  119c2df` in the upstream rars fork answered "Not a valid object name", so for
  two weeks this file named a rev the tree did not have, and the fork
  sat at 1b84503 plus its own 4 Sep work. 141 vendor commits and 115
  VENDORING.md rows had landed here in the meantime, over 28 paths and
  ~16,400 lines, five of them (memtrack.rs, rar50/blake2sp/simd.rs,
  rar50/write/stream.rs, recovery/gf16_fold.rs, the codec/rar50/
  directory) existing ONLY here, so the documented one-line re-vendor
  would have deleted working engine code. Reconciled by the 22 Aug
  shape: one commit on perf, 949349e, mirroring src/ from vendor at
  nzbfast ec623c71e, after checking vendor was a strict SUPERSET (every
  symbol in the fork's src resolves in the vendored tree; every fork
  commit since 1b84503 touching crates/rars/src was already
  hand-carried across). `sync-from-fork.sh` then re-mirrored from it
  with an empty `git diff vendor/rars/src`. See VENDORING.md.
  CORRECTION, same day: the fork DOES have a private durable home, and
  the 23 Aug note's "pushed" was accurate. The private fork remote
  carries `perf` at `119c2df` exactly. What is wrong is the CHECKOUT:
  the upstream rars fork had only the PUBLIC upstream configured, so the rev it
  was pushed to was never fetchable there and read as missing. The
  private remote is wired up as `mine` now and `git cat-file -t
  119c2df` answers. Read a "rev does not exist" in this file as an
  UNWIRED REMOTE first, not as a lost commit; listing the remote's
  branches costs seconds and settles it.
  The two lines had diverged at c586ae2, `mine/perf` carrying the
  23 Aug redo trio and the local `perf` carrying 1b84503, the fork's
  own 4 Sep work and this mirror, so 949349e could not fast-forward it.
  RECONCILED the same day and no longer open: merged with the ours
  strategy, which keeps the mirror's tree byte for byte, on the
  evidence that 119c2df's src is a strict SUBSET of it (no file and no
  symbol exists only there). Nothing was force-pushed and the redo trio
  stays reachable through the merge's second parent. The fork tip is
  258a508 and the private remote, this box and the second Mac all hold
  it, so the fork has a durable home again rather than living on one
  disk. Never push either line to the public upstream (TODO 11).
  258a508 also adds the repo-root rust-toolchain.toml the fork never
  had, so a bare `cargo` there resolves to 1.98.0 with clippy instead
  of a `stable` with no clippy component. It is outside the mirror's
  scope, so it does not move a vendored byte.
  8 Sep 2026: 30 vendor commits (~4,600 lines over 12 files, plus
  codec/rar50/probe_stats.rs which existed only here) had landed since
  the 258a508 sync, so this was drift again exactly as the line above
  warns. Ported by the same shape as 22 Aug and 7 Sep: one mirror commit
  on perf, 1fbe419, after checking the fork was 0 commits AHEAD of
  origin/perf and that no file existed only on the fork side - ie that
  the vendor was a strict superset. Fixtures were already identical.
  Then three candidates from the review's unmerged RAR5 shortlist landed ON
  TOP in the fork (eacb5c3 recovery shards, 606b146 two-queue Huffman,
  0f7acba worker reuse + a panic fix the candidate lacked), and
  sync-from-fork.sh --force re-mirrored from 10e8f5b. `--force` was
  needed only because the guard is date-based; it was safe on the
  mechanical proof that `diff -r` of the two src trees showed EXACTLY
  the three candidate files and nothing else, and the sync then changed
  exactly those three. Pushed and verified: the private remote answers
  10e8f5b for branch perf.
  A NOTE ON THE CHECKOUT, since this file has been wrong about it twice:
  the upstream rars fork was 23 commits BEHIND origin/perf when this started, so
  a first read made the fork look ~14,000 lines stale and the port look
  enormous. `git fetch` settled it in seconds. Fetch before you measure
  drift; a stale checkout and a lost commit look identical.
