fork: github.com/bitplane/rars, branch perf
last-synced-rev: 119c2df
last-synced-date: 2026-08-23
local-drift: none at the sync. On 22 Aug 2026 the whole vendored tree
  (104 local commits since the 24 Jul sync at 050758e, the NEON BLAKE2sp
  leaves among them) was ported INTO the fork as one commit, 1b84503 on
  branch perf, and sync-from-fork.sh then re-mirrored from it with an
  empty `git diff vendor/rars/src` as the proof. The fork checkout is
  the default RARS_FORK path, ~/Claude/rars; it is private and
  publishing stays deferred per TODO 11.
  Any vendor commit after this date is drift again: port it to the fork
  before the next sync, or the sync deletes it.
  23 Aug 2026: 1b84503 was never pushed and the ~/Claude/rars checkout
  that held it was gone, so origin/perf still sat at c586ae2. Re-cloned
  the private fork to ~/Claude/rars and re-did the port as three commits on
  perf (f251247 backlog to 15fb34e27^, f17ff11 the split-STORED CRC fix,
  119c2df the 22-23 Aug vendor commits), pushed; `diff -r` of src/ and
  tests/fixtures/ against the vendored tree is empty at 119c2df. The
  fork's rar50_fixtures.rs / rar50_stream_source.rs tests were moved to
  the pad-length and RAR 1.3-only-refused contracts there.
