/* SwiftPM wants a header beside the module map for a system-library target.
   The real declarations come in through the module map's path into the
   crate's own committed header; this file exists so the target has one. */
