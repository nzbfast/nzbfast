<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- A foreground service is exempt from being killed, not from the
         device suspending: with the screen off and no wakelock the CPU
         stops and the download stalls under a notification that still
         claims progress. Held only while the queue has work in it -
         EngineService.holdWake. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Read-only, and for two questions: is this network metered (the
         pause-on-metered setting) and what does the platform think its
         downstream rate is (the connection count a phone should open,
         DeviceProfile). Neither needs location and neither is a network
         change. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- extractNativeLibs=true: the engine ships as libnzbfast.so and is
         exec'd from nativeLibraryDir, which requires a real file on disk.
         Same mechanism as the proven WebView test APK. -->
    <application
        android:label="nzbfast"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <!-- cleartext: on-device mode talks to 127.0.0.1; remote daemons
             on a LAN are commonly plain http too. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Share-target for .nzb files. NZBs arrive under several
                 mime types in the wild; accept the usual suspects. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-nzb" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
            <!-- Shared text: nzblnk links pasted or shared from a browser. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- nzblnk: scheme links tapped anywhere. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nzblnk" />
            </intent-filter>
        </activity>
        <service
            android:name=".EngineService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
